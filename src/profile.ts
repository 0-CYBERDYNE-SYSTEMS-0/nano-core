export type FFTProfile = 'core';

export const FFT_PROFILE: FFTProfile = 'core';
export const FEATURE_FARM = false as const;
export const PROFILE_DETECTION = {
  source: 'hardcoded',
  reason: 'single-core',
} as const;
